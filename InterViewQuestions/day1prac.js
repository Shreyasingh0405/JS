
//1. **Find the Largest Element:**
   //- Write a function to find the largest element in an array.
   function largeElement(num){
     let large = ""
   }
   const result = largeElement([12,23,34,56,100,87])
   const solution = console.log(result)

//2. **Reverse an Array:**
  // - Write a function to reverse an array in-place.

  function rev(arr){
    let array =[]
    for(let i=arr.length-1;i>=0;--i){
        array+= arr[i]
    }
    return array
  }
  const result1 = rev([1,2,3,4,5])
  const solution1 = console.log(result1)

//3. **Remove Duplicates:**
   //- Write a function to remove duplicates from an array.

//4. **Find the kth Largest Element:**
  // - Write a function to find the kth largest element in an array.

//5. **Check for Subarray with Zero Sum:**
  // - Write a function to check if there is a subarray with a sum of zero.